{
  "name": "my-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
  "build": "next build",
  "export": "next export",
  "deploy": "npm run build && npm run export && gh-pages -d out"
  },
  "dependencies": {
    "framer-motion": "^11.2.10",
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
